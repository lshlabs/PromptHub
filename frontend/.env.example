# PromptHub 프론트엔드 환경변수 설정
# 이 파일을 .env.local(로컬) 또는 Render Environment Variables 참고용으로 사용하세요.

# API 설정 (반드시 설정 필요)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_INTERNAL_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google OAuth 설정
#
# Google Cloud Console 설정 방법:
# 1. https://console.cloud.google.com/ 접속
# 2. 프로젝트 선택 → APIs & Services → Credentials
# 3. OAuth 2.0 Client IDs 생성 (Web application)
# 4. 승인된 JavaScript 원본에 다음 추가:
#    - http://localhost:3000 (개발용)
#    - https://your-frontend.onrender.com (배포용)
# 5. 승인된 리디렉션 URI에 다음 추가:
#    - http://localhost:3000/api/auth/callback/google (개발용)
#    - https://your-frontend.onrender.com/api/auth/callback/google (배포용)

# Google OAuth 클라이언트 (현재 코드 기준 필수 키명)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id-from-console
GOOGLE_CLIENT_SECRET=your-google-client-secret-from-console

# NextAuth 설정 (Google 로그인 사용 시 필수)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-a-long-random-secret

# 분석 도구 (선택사항)
# NEXT_PUBLIC_GA_ID=your-google-analytics-id

# API 키들 (필요시 주석 해제)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# NEXT_PUBLIC_OPENAI_API_KEY=

# 개발 환경 설정
NODE_ENV=development
