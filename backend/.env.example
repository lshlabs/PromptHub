# PromptHub 백엔드 환경변수 템플릿
# 이 파일은 아래 용도로 참고하세요.
# - 로컬 개발 (`backend/.env`)
# - 프로덕션 배포 (Render Backend Environment Variables)

# Django 설정
# 배포(Render) 예시: config.settings_prod
# 로컬 개발 예시   : config.settings_dev (프로젝트 설정 방식에 따라 선택)
DJANGO_SETTINGS_MODULE=config.settings_prod

# Django Secret Key
# settings_base / settings_prod 키명 호환을 위해 두 키에 같은 값을 넣는 것을 권장합니다.
SECRET_KEY=replace-with-a-long-random-secret
DJANGO_SECRET_KEY=replace-with-a-long-random-secret

# 디버그 / 로그
DEBUG=False
DJANGO_DEBUG=False
DJANGO_LOG_LEVEL=INFO
PYTHONUNBUFFERED=1

# 데이터베이스
# Render 배포 시: Render Postgres Internal Database URL 사용 권장
# 로컬에서 Render Postgres에 직접 접속해 관리 명령 실행할 때는 External URL(+ sslmode=require) 사용
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME

# 허용 호스트 / CORS / CSRF
# 쉼표(,)로 여러 도메인 입력 가능
DJANGO_ALLOWED_HOSTS=your-backend.onrender.com
CORS_ALLOWED_ORIGINS=https://your-frontend.onrender.com
CSRF_TRUSTED_ORIGINS=https://your-frontend.onrender.com

# Cloudinary (프로필 이미지/업로드 파일 저장)
# 배포 환경에서는 설정 권장 (미설정 시 로컬 파일 시스템 저장으로 유실 가능)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# 선택 (필요 시)
# PYTHON_VERSION=3.12.9
